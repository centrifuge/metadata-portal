name: CLI

on:
  pull_request:
    branches:
      - master
    paths:
      - "cli/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 🛎 Checkout
        uses: actions/checkout@v4

      - name: 🔧 Install rust dependencies
        uses: ./.github/workflows/rust-install

      - name: 🧹 Clean Cargo Git checkouts and OpenCV build
        run: |
          rm -rf ~/.cargo/git/checkouts
          rm -rf ~/.cargo/registry/src/*/opencv-*
          rm -rf target

      - name: Cargo fmt
        run: cargo fmt --all -- --check

      - name: Clean cargo artifacts
        run: |
          rm -f Cargo.lock
          rm -rf target
          rm -rf ~/.cargo/git/checkouts/parity-signer*
          rm -rf ~/.cargo/registry
          cargo clean
          cargo update -p opencv

      - name: Debug OpenCV version
        run: cargo tree | grep opencv

      - name: Confirm regenerated cvt_color binding
        run: |
          grep -A 10 "pub fn cvt_color" target/debug/build/opencv-*/out/opencv/imgproc.rs || echo "not found"

      - name: Cargo check --locked
        run: cargo check

      - name: Cargo clippy
        run: cargo clippy -- -D warnings

      - name: Cargo test
        run: cargo test
